# Нутрико — работен лог

> **Източник на истината за прогреса на проекта.** Обновява се в края на всяка работна сесия.
> Най-новото е най-отгоре в „Дневник". Свършеното от „Предстои" се мести в „Дневник" с дата.

**Продукция:** https://nutriko.fit · Vercel проект `nutrico` · Supabase `nutrico` (`xrdanumtjbrpkrjtvyjx`)
**Деплой:** `npx vercel --prod --yes` от папката на проекта (билд: `npm run build`)

---

## ⏭️ Предстои

- [ ] **Фактуриране → посока Eltrade Cloud (cloud.eltrade.com), НЕ правим наш модул**: за обекта така или иначе трябва POS софтуер + касов апарат — облачният софтуер на Eltrade покрива и фактурирането, наш модул би дублирал функции (решение на Юлиан, 2026-07-17, след консултация със счетоводителя предстои тест). **Чеклист при теста на Eltrade** (изискванията, които обсъдихме): проформа с отделна номерация; 10-разрядна поредна номерация; текст „чл. 113, ал. 9 ЗДДС" докато няма ДДС регистрация + пълни ДДС редове след нея (BG200708161); EUR + дуално в лв.; експорт за счетоводителя; архив. Фирмените данни (ЕИК, IBAN УниКредит, адрес, МОЛ) → `~/.claude/personal-data.md`.
- [ ] **AI обработка на документи (след няколко седмици, идея на Юлиан 2026-07-17)**: конвертиране на документи + автоматично разчитане и попълване на фактури (напр. входящи фактури от доставчици → разчитане → структурирани данни, евентуално връзка със съставките/доставните цени в калкулатора). Ще се уточни обхватът, когато дойде времето.

- [ ] **Организация на снимковите файлове** — локалният архив да се подреди по директории за всеки продукт (`nutriko-photos/products/<slug>/` — за Крема Мус вече е така); идея: архивът в Google Drive за достъп отвсякъде
- [ ] **Крема Мус — още кадри?** — сурови 2.jpg / 4.jpg / 5.jpg чакат в папката, ако искаме трети кадър в галерията
- [ ] **Facebook/Instagram** — насрочване на стартовия пакет в Business Suite Planner; Reels; покани към приятели (pre-launch набиране на последователи)
- [ ] **Останали съставки без цена** — 145 бр. чакат цени в админа (за пълна себестойност на рецептите)
- [ ] **GitHub резервно копие** — частно репо `nutrico` под `yuliannetiliev-sys` (там са само apex-cg и apex-landing) + remote + push; БЕЗ Vercel Git интеграция (деплоят остава през CLI). Отложено от Юлиан за по-късно (2026-07-16)

---

## 📖 Дневник

### 2026-07-16
- **Мулти-админ (втори администратор — Самуил, samuil.slavev@gmail.com, пълен достъп)**: нова функция `public.is_admin()` (централен списък имейли) + 10-те RLS политики пренасочени към нея; в кода `ADMIN_EMAIL` → `ADMIN_EMAILS` (`lib/supabase.ts` + `middleware.ts`). **Нов админ занапред = имейл на 3 места**: `is_admin()` (SQL), `ADMIN_EMAILS` ×2 (код) + акаунт от Supabase Dashboard → Authentication → Add user. Акаунтът на Самуил се създава ръчно от Юлиан в Dashboard-а.
- **Finesse страница v2 (по брифа `Finesse_1_godina.docx`)**: ново съдържание и структура — hero „Грижата се разпознава във всеки детайл" + обща AI снимка на двете чашки (16:9), карти по брифа (без брашно, белгийски шоколад, „В него ще откриеш", badge чипове), **макроси на стандартно парче ≈155 g + задължителна бележка за чашките**, секции „Защо цели сушени фурми?" и „Десерт с добавена стойност" (✓ списък), пълен поздрав към Finesse Medical SPA, CTA „Finesse × Nutriko" с бутон към finesse-spa.bg; анкетата с миниатюри вместо emoji. URL остана `/finesse` (QR-ът е отпечатан; брифът предлагаше друг — отхвърлено съзнателно).
- **Finesse: реални снимки на чашките + A4 флайер за ламиниране** — кадрите от Юлиан (Cocos1/Mus1) → Higgsfield (чашката непроменена, светъл крем/мрамор фон) → на страницата (`events/finesse/kokosova.webp`, `krema-mus.webp`) + `flyer-a4.png` (светъл фон: двете чашки + QR, `scripts/make-finesse-flyer.mjs`, QR-ът декодиран ✓).
- **Събитийна страница `/finesse`** (1 г. Finesse SPA — 50+50 чашки Кокосова/Крема Мус): поздрав, двете чашки с реални макроси от калкулатора + съставки + алергени, „Защо без бяла захар" с /polezno линкове, **анкета „Коя ти хареса повече?"** (таблица `event_votes`, анонимно, localStorage guard; тествана end-to-end и нулирана), CTA към менюто; noindex. Плюс **QR карта за печат** (A6 + A4×4, `scripts/make-finesse-qr.mjs`, QR-ът верифициран с декодиране) в `nutriko-photos/events/finesse/`. Шаблон за бъдещи фирмени поръчки.
- **Git baseline** — първи истински комит (`15bb7c9`, 66 файла) върху скелетния initial commit; `.env*` е извън историята. Оттук нататък: комит в края на всяка сесия.
- **Създаден този лог** (`PROJECT_LOG.md`) — източник на истината за прогреса.
- **Торта Крема Мус (id 44) — снимки публикувани в менюто**: сурови кадри от телефона → Higgsfield обработка (тортата непроменена, кремав студиен фон/мрамор/бяла керамика като останалите) → `main.webp` (корица, цяла торта) + `piece.webp` (парче) в `product-images/torta-krema-mus/`, ~200 KB всяка. Slug оправен от `produkt-1m93x5` → `torta-krema-mus`.
- **Фикс: качване на снимки през админа** — файлове > ~4.5 MB гърмяха („An unexpected response…") заради платформен лимит на Vercel (12 MB конфигът не помага). Решение: клиентска компресия преди качване (`lib/compressImage.ts` — 2048px WebP), закачена на 4-те upload места (продукти, hero банер, статии, текстове). Вече може да се качва директно от телефона.
- **Фикс: „Server Components render" грешка при запис на съставка** — ГИ полето е цяло число (`smallint`), а въведено `0.5` гърмеше с маскирана грешка. Сега ГИ се закръгля на входа, а `createIngredient`/`updateIngredient` връщат разбираеми български грешки вместо да хвърлят (напр. дублирано име).

### 2026-06-21 · Социални мрежи (pre-launch)
- Facebook страница **facebook.com/nutrikobg** („Нутрико (Шейкъри Кейкъри)") — профилна (кафе икона), корица (lifestyle с кучето), био, локация; първи пост публикуван и закачен.
- Instagram **@nutrikobg** — създаден, свързан с FB в Business portfolio „Нутрико"; 2-ма екипни админи поканени; първи IG пост публикуван.
- Съдържателни пакети: `nutriko-facebook/facebook-starter-pack.md` (10 поста + 2-седм. календар) и `instagram-content-pack.md` (feed/карусели/Reels/Stories + хаштаг сетове).

### юни 2026 · Сайт и съдържание
- **/polezno redesign**: категорийни секции, hero банер (lifestyle, избираем от админа), 14 AI корици на статиите, breadcrumbs + JSON-LD, „Свързани статии", бутони за навигация, промо блок на началната, помощни линкове в менюто (вкл. поправен „?" линк за ГИ след смяна на slug-ове).
- **Статии — SEO полета**: отделни SEO заглавие / Meta описание / кратък текст за картата + подредба на статиите с ▲▼ направо от списъка.
- **Loading skeletons** за force-dynamic маршрутите (`/menu`, `/polezno`, …) — навигацията на телефон изглеждаше „счупена" без тях.

### 2026-06-13 · Старт
- Supabase проект `nutrico`: схема (продукти/рецепти/съставки/алергени/статии/настройки), 218 съставки импортирани.
- Сайт: публично меню с макроси/ГИ/алергени + админ калкулатор на рецепти и себестойност.

---

## 🧭 Конвенции (накратко)

- **Админ мутации връщат `{ ok, error }`** — не `throw` (production маскира хвърлените грешки с безполезното „Server Components render…").
- **ГИ = цяло число** (`smallint`; калкулаторът така или иначе закръгля) — закръгля се на входа.
- **Съставки с `unit='бр.'`** — задължително `grams_per_unit`; макроси и цена се въвеждат **на 100 g**.
- **Снимки на продукти**: телефонните оригинали в `nutriko-photos/products/<slug>/`; в storage отиват **2048px WebP** (`product-images/<slug>/`); EXIF/GPS се маха преди публичен bucket.
- **Смяна на slug** (статия или продукт) — провери твърдо-кодирани линкове (менюто, телата на статиите).
- **Supabase Storage REST** със `sb_secret_` ключ иска header `apikey:` (не `Authorization: Bearer`).
- **Край на всяка сесия**: обнови този лог (Дневник + Предстои) и направи `git commit`.
